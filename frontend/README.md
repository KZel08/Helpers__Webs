
  # App Layout UI Design

  This is a code bundle for App Layout UI Design. The original project is available at https://www.figma.com/design/DaoyzPx63cVDZy4trTY0LV/App-Layout-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  